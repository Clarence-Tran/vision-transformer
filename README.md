# vision-transformer
Step-by-step implementation of Vision Transformer (ViT), including image patch embedding, positional encoding, multi-head self-attention, Transformer encoder blocks, and classification head, with applications demonstrated on benchmark datasets.
